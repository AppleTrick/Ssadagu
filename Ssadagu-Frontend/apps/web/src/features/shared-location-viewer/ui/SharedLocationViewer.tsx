interface SharedLocationViewerProps {
  lat: number;
  lng: number;
  label?: string;
}

const SharedLocationViewer = (props: SharedLocationViewerProps) => {
  return null;
};

export default SharedLocationViewer;
