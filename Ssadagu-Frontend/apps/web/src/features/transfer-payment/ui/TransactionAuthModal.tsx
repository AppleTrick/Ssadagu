interface TransactionAuthModalProps {
  isOpen: boolean;
  onSuccess: () => void;
  onClose: () => void;
}

const TransactionAuthModal = (props: TransactionAuthModalProps) => {
  return null;
};

export default TransactionAuthModal;
