import type { Transaction } from '../model/types';

interface TransactionBubbleProps {
  transaction: Transaction;
}

const TransactionBubble = ({ transaction }: TransactionBubbleProps) => {
  return null;
};

export default TransactionBubble;
